using System;

namespace AppInstaller
{
    class Program
    {
        static void Main(string[] args)
        {
            // ファイル一覧を作る？
            // 更新先プロジェクトを見る
            // 　更新先はZipで置いてあるのかな？
            // テンポラリファイルとそれ以外も定義しないといけないな
            
            // 更新先から取得
            // 現状の定義で削除
        }
    }
}
